apiVersion: v1
kind: Service
metadata:
  name: bestify-redis
  namespace: bestify
  labels:
    app: bestify
    role: redis
    tier: db
spec:
  selector:
    app: bestify
    role: redis
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
